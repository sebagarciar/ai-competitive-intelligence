# Dashboard-only requirements for read-only cloud demo.
# Pipeline packages (torch, ingestion, NLP) are not needed here —
# all data is pre-computed and stored in data/intelligence.db.

python-dotenv>=1.0.0
numpy>=1.24.0
pandas>=2.0.0
jinja2>=3.1.0
plotly>=5.20.0
fpdf2>=2.7
mistune==2.0.5
